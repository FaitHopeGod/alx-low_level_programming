#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 48; n <= 57; n++)
	{
		putchar(n);
		if (n == 57)
			break;
		putchar(',');
		putchar(32);
	}
	putchar('\n');
	return (0);
}
