#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 48; x <= 57; x++)
	{
		putchar(n);
		if (x == 57)
			break;
		putchar(',');
		putchar(32);
	}
	putchar('\n');
	return (0);
}
